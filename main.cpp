#include"Functions.h"
#include"Sign.h"
int main()
{
	initial();
	Menu();
	/*int op;
	while (1)
	{
		cout << "输入1存快递，2取快递，3查询快递柜使用情况，4查询货物存取情况，5查询过期快递" << endl;
		cin >> op;
		if (op == 1)
			Put();
		else if (op == 2)
			Check();
		else if (op == 3)
			checkBox();
		else if (op == 4)
			checkGoods();
		else if (op == 5)
			checkOvertime();
		system("pause");
		system("cls");
	}*/

	return 0;
}